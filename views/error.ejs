<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram | Page Not Found</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <style>
        body {
            background-color: #18181B;
            color: white;
            font-family: 'Segoe UI', sans-serif;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .section {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        
        .heading {
            font-size: 6rem;
            font-weight: bold;
            color: #e5e7eb;
            margin-bottom: 1rem;
        }
        
        .subheading {
            font-size: 2rem;
            font-weight: bold;
            color: #e5e7eb;
            margin-bottom: 1rem;
        }
        
        .text {
            font-size: 1.2rem;
            color: #d1d5db;
            margin-bottom: 2rem;
            text-align: center;
        }
        
        .btn {
            padding: 0.75rem 1.5rem;
            font-size: 1rem;
            font-weight: bold;
            color: #fff;
            background-color: royalblue;
            border-radius: 0.5rem;
            transition: background-color 0.3s ease;
        }
        
        .btn:hover {
            background-color: rgba(65, 105, 225, 0.726);
        }
        
        @media (max-width: 768px) {
            .heading {
                font-size: 4rem;
            }
            .subheading {
                font-size: 1.5rem;
            }
            .text {
                font-size: 1rem;
            }
            .btn {
                font-size: 0.9rem;
            }
        }
    </style>
</head>

<body>

    <section class="section">
        <div class="container">
            <div class="text-center">
                <h1 class="heading">404</h1>
                <p class="subheading">Page not found!</p>
                <p class="text">Sorry, we can't find that page. You'll find lots to explore on the home page.</p>
                <a href="/login" class="btn">Back to Homepage</a>
            </div>
        </div>
    </section>

</body>

</html>