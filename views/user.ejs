<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram | Already Registered</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background-color: #18181B;
            color: white;
            font-family: 'Segoe UI', sans-serif;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        .section {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        
        .heading {
            font-size: 2rem;
            font-weight: bold;
            color: #fff;
            margin-bottom: 1rem;
        }
        
        .text {
            font-size: 1.2rem;
            color: #d1d5db;
            margin-bottom: 2rem;
            text-align: center;
        }
        
        .btn {
            padding: 0.75rem 1.5rem;
            font-size: 1rem;
            font-weight: bold;
            color: #fff;
            background-color: royalblue;
            border-radius: 0.5rem;
            transition: background-color 0.3s ease;
        }
        
        .btn:hover {
            background-color: rgba(65, 105, 225, 0.685);
        }
        
        @media (max-width: 768px) {
            .heading {
                font-size: 1.5rem;
            }
            .text {
                font-size: 1rem;
            }
            .btn {
                font-size: 0.9rem;
            }
        }
    </style>
</head>

<body class=" min-h-screen flex justify-center items-center">
    <section class="section">
        <div class="container">
            <div class="text-center">
                <h1 class="heading">Already Registered</h1>
                <p class="text">It looks like you've already registered. Please log in to continue.</p>
                <a href="/login" class="btn">Log In</a>
            </div>
        </div>
    </section>
</body>

</html>